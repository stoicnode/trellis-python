# trellis's own audit configuration (SPEC §6.5) — dogfooding the
# deterministic audit. Keys stay in the schema's casing.
source:
  exclude:
    # Fixed validation fixtures (trellis-e924, corpus/): intentional clones,
    # erosion, cycles, and parse failures — audit inputs, not product source.
    - "corpus/**"
    # Generated parser tables and their upstream tokenizer are audit runtime
    # assets, not handwritten product behavior (trellis-0000).
    - "src/python/grammar/**"
